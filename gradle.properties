# Done to increase the memory available to gradle.
	org.gradle.jvmargs=-Xmx3G
    org.gradle.parallel = true

# Mod Properties
	version = 0.2.1
	maven_group = org.vfast
	archives_base_name = backrooms